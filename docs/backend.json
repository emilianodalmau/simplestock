
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Firebase Blank Slate application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "firstName": {
          "type": "string",
          "description": "The user's first name."
        },
        "lastName": {
          "type": "string",
          "description": "The user's last name."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "phone": {
          "type": "string",
          "description": "Contact phone number of the user."
        },
        "address": {
          "type": "string",
          "description": "Address of the user."
        },
        "photoURL": {
          "type": "string",
          "description": "The URL of the user's profile picture."
        },
        "role": {
          "type": "string",
          "description": "The role of the user, which determines their permissions.",
          "enum": [
            "administrador",
            "editor",
            "visualizador"
          ]
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName"
      ]
    },
    "Category": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Category",
      "type": "object",
      "description": "Represents a product category to group items.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Category entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the category."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the category."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product in the inventory.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "code": {
          "type": "string",
          "description": "A unique, auto-generated code for the product."
        },
        "name": {
          "type": "string",
          "description": "The name of the product."
        },
        "categoryId": {
          "type": "string",
          "description": "Reference to the Category this product belongs to."
        },
        "supplierId": {
          "type": "string",
          "description": "Reference to the Supplier of this product."
        },
        "price": {
            "type": "number",
            "description": "The price per unit of the product."
        },
        "minStock": {
          "type": "number",
          "description": "The minimum stock level for the product."
        },
        "unit": {
          "type": "string",
          "description": "The unit of measurement for the product stock.",
          "enum": [
            "unidades",
            "litros",
            "kilos",
            "metros",
            "gramos",
            "cajas"
          ]
        },
        "isArchived": {
          "type": "boolean",
          "description": "If true, the product is archived and not available for new transactions."
        }
      },
      "required": [
        "id",
        "code",
        "name",
        "categoryId",
        "supplierId",
        "price",
        "minStock",
        "unit"
      ]
    },
    "Deposit": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Deposit",
      "type": "object",
      "description": "Represents a physical location where products are stored (e.g., warehouse, storage room).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Deposit entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the deposit."
        },
        "description": {
          "type": "string",
          "description": "A brief description or location details of the deposit."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Supplier": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Supplier",
      "type": "object",
      "description": "Represents a product supplier.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Supplier entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the supplier."
        },
        "contact": {
          "type": "string",
          "description": "Contact person at the supplier."
        },
        "phone": {
          "type": "string",
          "description": "Contact phone number of the supplier."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "InventoryStock": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "InventoryStock",
      "type": "object",
      "description": "Represents the stock of a specific product in a specific deposit. The document ID is a composite key: `${productId}_${depositId}`.",
      "properties": {
        "productId": {
          "type": "string",
          "description": "Reference to the Product."
        },
        "depositId": {
          "type": "string",
          "description": "Reference to the Deposit where the stock is located."
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the product in this stock location."
        },
        "lastUpdated": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of the last stock update."
        }
      },
      "required": [
        "productId",
        "depositId",
        "quantity",
        "lastUpdated"
      ]
    },
    "StockMovement": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StockMovement",
      "type": "object",
      "description": "Represents a single stock movement event (remito), which can be an entry or an exit, involving multiple products.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the StockMovement document."
        },
        "remitoNumber": {
            "type": "string",
            "description": "A user-defined unique number for the delivery note (remito)."
        },
        "type": {
          "type": "string",
          "description": "The type of movement.",
          "enum": ["entrada", "salida"]
        },
        "depositId": {
          "type": "string",
          "description": "Reference to the Deposit where the movement occurs."
        },
        "depositName": {
          "type": "string",
          "description": "Denormalized name of the deposit for quick display."
        },
        "actorId": {
          "type": "string",
          "description": "Optional reference to the User (for salidas) or Supplier (for entradas)."
        },
        "actorName": {
          "type": "string",
          "description": "Denormalized name of the user or supplier."
        },
        "actorType": {
          "type": "string",
          "description": "Specifies if the actor is a user or supplier.",
          "enum": ["user", "supplier"]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the movement was created."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who registered the movement."
        },
        "items": {
          "type": "array",
          "description": "A list of all products and quantities involved in this movement.",
          "items": {
             "$ref": "#/entities/StockMovementItem"
          }
        },
        "totalValue": {
          "type": "number",
          "description": "The total monetary value of the movement."
        }
      },
      "required": ["id", "type", "depositId", "createdAt", "userId", "items", "totalValue"]
    },
    "StockMovementItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StockMovementItem",
      "type": "object",
      "description": "Represents a single product line within a StockMovement.",
      "properties": {
        "productId": { "type": "string" },
        "productName": { "type": "string" },
        "quantity": { "type": "number" },
        "unit": { "type": "string" },
        "price": { "type": "number" },
        "total": { "type": "number" }
      },
      "required": ["productId", "productName", "quantity", "unit", "price", "total"]
    },
    "Counter": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Counter",
      "type": "object",
      "description": "Represents an atomic counter used for generating sequential numbers.",
      "properties": {
        "lastNumber": {
          "type": "number",
          "description": "The last number that was generated by the counter."
        }
      },
      "required": ["lastNumber"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. User documents are created via Firebase Authentication.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/categories/{categoryId}",
        "definition": {
          "entityName": "Category",
          "schema": {
            "$ref": "#/backend/entities/Category"
          },
          "description": "Stores product categories.",
          "params": [
            {
              "name": "categoryId",
              "description": "The unique identifier of the category."
            }
          ]
        }
      },
      {
        "path": "/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Stores individual products.",
          "params": [
            {
              "name": "productId",
              "description": "The unique identifier of the product."
            }
          ]
        }
      },
      {
        "path": "/deposits/{depositId}",
        "definition": {
          "entityName": "Deposit",
          "schema": {
            "$ref": "#/backend/entities/Deposit"
          },
          "description": "Stores warehouses or physical locations for inventory.",
          "params": [
            {
              "name": "depositId",
              "description": "The unique identifier of the deposit."
            }
          ]
        }
      },
      {
        "path": "/suppliers/{supplierId}",
        "definition": {
          "entityName": "Supplier",
          "schema": {
            "$ref": "#/backend/entities/Supplier"
          },
          "description": "Stores product suppliers.",
          "params": [
            {
              "name": "supplierId",
              "description": "The unique identifier of the supplier."
            }
          ]
        }
      },
      {
        "path": "/inventory/{inventoryStockId}",
        "definition": {
          "entityName": "InventoryStock",
          "schema": {
            "$ref": "#/backend/entities/InventoryStock"
          },
          "description": "Stores stock levels for each product in various deposits. The ID is a composite `${productId}_${depositId}`.",
          "params": [
            {
              "name": "inventoryStockId",
              "description": "The composite identifier for the stock record."
            }
          ]
        }
      },
       {
        "path": "/stockMovements/{movementId}",
        "definition": {
          "entityName": "StockMovement",
          "schema": {
            "$ref": "#/backend/entities/StockMovement"
          },
          "description": "Stores stock movement documents (remitos).",
          "params": [
            {
              "name": "movementId",
              "description": "The unique identifier of the movement."
            }
          ]
        }
      },
      {
        "path": "/counters/{counterId}",
        "definition": {
          "entityName": "Counter",
          "schema": {
            "$ref": "#/backend/entities/Counter"
          },
          "description": "Stores atomic counters for sequential number generation.",
          "params": [
            {
              "name": "counterId",
              "description": "The unique identifier of the counter (e.g., 'remitoCounter')."
            }
          ]
        }
      }
    ]
  }
}
